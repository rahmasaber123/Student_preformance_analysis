# For Streamlit Community Cloud:
# Settings → Secrets → paste this content with your real URI.
# For local dev, use `.env` instead — secrets.toml is only for deployment.

MONGO_URI = "mongodb+srv://USER:[email protected]/?retryWrites=true&w=majority"
